package me.cybermaxke.elementarrows.common.recipe;

public interface RecipeManager {

	/**
	 * Adds a default recipe to the recipe manager.
	 * 
	 * @param recipe the recipe
	 */
	void addDefault(Recipe recipe);

}